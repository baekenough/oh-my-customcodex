---
title: "R015: Intent Transparency Rules"
type: rule
updated: 2026-05-24
sources:
  - .claude/rules/MUST-intent-transparency.md
related:
  - [[r007]]
  - [[r009]]
  - [[r010]]
  - [[r019]]
  - [[r021]]
---

# R015: Intent Transparency Rules

Display routing reasoning when delegating to agents so users always know which agent was selected, why, and how to override.

## Overview

When the orchestrator routes a task to a specialist agent, it must show the detection result including agent name, confidence level, and reason. Users can override with `@{agent-name} {command}`. This rule pairs with [[r007]] (identification) and [[r019]] (ontology-RAG enrichment).

## Priority

**MUST** — Enforced. Transparent routing prevents black-box agent delegation and enables informed user overrides.

## Key Requirements

**Display format:**
```
[Intent Detected]
├── Input: "{user input}"
├── Agent: {detected-agent}
├── Confidence: {percentage}%
└── Reason: {explanation}
```

**Confidence thresholds:**

| Confidence | Action |
|------------|--------|
| >= 90% | Auto-execute with display |
| 70–89% | Request confirmation, show alternatives |
| < 70% | List options for user to choose |

**Detection factors:**

| Factor | Weight |
|--------|--------|
| Keywords | 40% |
| File patterns | 30% |
| Action verbs | 20% |
| Context | 10% |

**Override:** `@{agent-name} {command}` bypasses detection entirely.

**Agent triggers:** Defined in `.codex/skills/intent-detection/patterns/agent-triggers.yaml`.

## Structured Question Failure Discipline

If `AskUserQuestion`, `omx question`, or another structured question surface fails, the agent must retry once with the smallest valid one-question shape. If that surface is unavailable, ask exactly one concise plain-text question and preserve the active workflow. A rejected or malformed question tool call is not permission to abandon the user's selected workflow.

This discipline is especially important for deep-interview and pipeline flows where a failed question renderer can otherwise erase the user's directive.

## Destructive Operation Approval Persistence

When the user explicitly approves a destructive operation for the same category and target in the same session, follow-up operations in that exact scope do not require re-confirmation. The agent still emits an advisory warning and must ask again for a different target, different operation category, or catastrophic git actions such as `git reset --hard`, `git clean -fd`, force pushes to shared branches, or unmerged `git branch -D`.

This is an advisory model-behavior rule. It cannot suppress Codex or Claude runtime permission prompts. If platform prompts keep recurring for the same approved command, surface the scoped settings/permission-rule workaround for that command instead of re-asking the same high-level question.

## User Directive Persistence

When a user explicitly names a tool, skill, or workflow (e.g., "use /pipeline auto-dev", "always run tests with bun test"), this preference persists for the entire session — including after autonomous mode transitions.

**Persistence triggers:**

| User Statement Pattern | Persistence Scope |
|------------------------|-------------------|
| "use X for development" | Entire session |
| "always / every time" | Entire session |
| "from now on" | Entire session + memory save candidate |
| "for this task" | Current task only |
| Named slash command | Subsequent similar invocations |

**Cycle Start Self-Check:**

At the start of every work cycle (issue, task, release, or autonomous sub-loop):
1. Review recent user messages in the conversation
2. Identify any named tool/skill/workflow directives
3. Apply those directives unless explicitly rescinded
4. If unsure whether a directive applies, default to the stated preference

**Anti-pattern:** Treating autonomous mode as a clean slate that discards earlier user preferences. Autonomous mode means "continue without per-step confirmation" — NOT "reset user directives".

```
❌ WRONG: Cycle 2 ignores user's "간결하게" preference from Cycle 1
✓ CORRECT: Directive persists — carry forward until user changes it
```

## Enforcement

Prompt-based per [[r021]]. Advisory only, with structured-question retry discipline.

## Relationships

- **Related rules**: [[r007]], [[r009]], [[r010]], [[r019]]
- **Affects**: All routing skills (secretary-routing, dev-lead-routing, de-lead-routing, qa-lead-routing), autonomous mode cycles

## Sources

- `.codex/rules/MUST-intent-transparency.md` — rule definition
- Issue #869 — user directive persistence in autonomous mode (2026-04-15)
- Issue #1387 — generalized approval persistence for repeated destructive operations
