---
title: "R008: Tool Identification Rules"
type: rule
updated: 2026-07-18
sources:
  - .codex/rules/MUST-tool-identification.md
related:
  - [[r007]]
  - [[r009]]
  - [[r010]]
  - [[r020]]
  - [[r021]]
---

# R008: Tool Identification Rules

Every tool call MUST be prefixed with agent and model identification so every action is traceable.

## Overview

Tool identification pairs with [[r007]] (agent identification) to create full traceability. Users see not just which agent is active but precisely which tool it is calling and on what target. The `[N]` prefix system for parallel spawns enables correlation with the Running display in Claude Code.

## Priority

**MUST** — Enforced. Missing identification prefix on tool calls requires immediate correction. PostCompact hook re-injects this rule.

## Key Requirements

**Format:**
```
[agent-name][model] → Tool: <tool-name>
[agent-name][model] → Target: <file/path/url>
```

**Model shorthand:** `opus`, `sonnet` (default), `haiku`

**Short response discipline:** Quick diagnostics and brief checks are not exempt. If a visible response will be followed by a tool call, show the `[agent][model] → Tool` and target prefix before the call even when the natural-language update is one sentence.

**Required-parameter completeness:** Prefix/announce and the actual tool payload are separate obligations. Before invoking a tool, verify all schema-required parameters are present. Canonical example: `AskUserQuestion`/structured-question calls must include a non-empty `questions` array; a prefix without required payload fields is not a valid R008-compliant call.

**Tier-3 interaction tools:** R008 applies to interaction/planning tools too (`AskUserQuestion`, `request_user_input`, `TodoWrite`/`update_plan`, `EnterPlanMode`, `ExitPlanMode`). Skill invocation is the exception because the R007 integrated header identifies the skill surface.

**Tool category verbs:**

| Category | Tools | Verb |
|----------|-------|------|
| File Read | Read, Glob, Grep | Reading / Searching |
| File Write | Write, Edit | Writing / Editing |
| Network | WebFetch | Fetching |
| Execution | Bash, Agent | Running / Spawning |

**Parallel spawn prefix rule:** When spawning 2+ agents, each Agent tool's `description` parameter MUST include a `[N]` 1-indexed prefix for Running display correlation:
```
Agent(description: "[1] Go code review", subagent_type: "lang-golang-expert")
Agent(description: "[2] Python code review", subagent_type: "lang-python-expert")
```

Single agent spawns do NOT use the `[N]` prefix.

The source retains its consolidated tool-identification examples inside an optional `DETAIL` block. This context compaction does not relax prefixing, required-payload, or parallel-description requirements.

## Enforcement

Advisory PostToolUse hook + PostCompact re-injection per [[r021]].

## Relationships

- **Related rules**: [[r007]], [[r009]], [[r010]], [[r020]]
- **Affects**: All tool calls across all agents

## Claude Workflow attribution

R008 now notes that Claude Code v2.1.174 fixed Workflow `agent()` attribution headers. Workflow fan-out should still be reasoned about with the same per-agent identification discipline as parallel Agent spawns, while Codex-native subagents continue to require explicit OMX `agent_type` routing.

## Sources

- `.codex/rules/MUST-tool-identification.md` — rule definition
