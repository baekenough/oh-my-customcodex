# 릴리즈 계획 — 2026-05-13 갱신

> 출처: 2026-05-13 기준 `verify-done` 및 upstream port 오픈 이슈
> 제외된 이슈 (이미 오픈 PR에 포함): 없음

## v0.4.16 릴리즈 계획

**예상 범위**: M | **이슈**: 5 | **병렬 가능**: 4

| # | 우선순위 | 규모 | 제목 | 의존성 |
|---|----------|------|------|--------|
| #1303 | P2 | S | `/goal` workflow namespace collision port | 없음 |
| #1304 | P3 | XS | CC v2.1.139 onboarding commands | 없음 |
| #1306 | P3 | XS | Hook `args: string[]` exec form migration review | 없음 |
| #1307 | P3 | S | PostToolUse hook `continueOnBlock` compatibility | #1306 |
| #1308 | P3 | S | Claude Code v2.1.140 compatibility review | #1303, #1304 |

### 구현 순서
1. #1303 — goal workflow의 사용자 호출명을 `/omcustomcodex:goal`로 분리하고 source/template/help/wiki/ontology 참조를 동기화합니다. (권장 에이전트: general-purpose)
2. #1304 — Claude Code v2.1.139의 `claude agents`, `/scroll-speed`, `claude plugin details`, `/mcp` reconnect 동작을 호환성 및 온보딩 문서에 반영합니다. (권장 에이전트: arch-documenter)
3. #1306 — hook `args: string[]` exec form을 검토하고 기존 shell command hook 유지 기준을 버전 호환성 가이드에 기록합니다. (권장 에이전트: arch-documenter)
4. #1307 — high-signal PostToolUse advisor에 `continueOnBlock`를 적용하고 hard-block exit를 `2`로 정렬합니다. (권장 에이전트: general-purpose)
5. #1308 — Claude Code v2.1.140 release notes의 Codex port 영향도를 버전 호환성 가이드와 architecture matrix에 반영합니다. (권장 에이전트: arch-documenter)

### 참고 사항
- `.codex/skills/goal/` 및 `templates/.claude/skills/goal/` 디렉터리는 유지하고 프론트매터 `name`과 문서 엔트리포인트만 네임스페이스화합니다.
- native `/goal`은 런타임 completion tracker로 남기고, oh-my-customcodex workflow는 `/omcustomcodex:goal`로 호출합니다.
- v2.1.139+ hook `continueOnBlock`는 model-visible recovery signal에만 사용하고, low-signal stderr advisory는 유지합니다.
- v2.1.140의 relaxed `subagent_type` matching은 허용하되 repo 문서는 canonical kebab-case를 유지합니다.
- 호환성 및 hook 변경 중심 릴리스이므로 패치 버전으로 계획합니다.

## 요약

| 릴리즈 | 이슈 수 | 규모 | P1 | P2 | P3 |
|--------|---------|------|----|----|-----|
| v0.4.16 | 5 | M | 0 | 1 | 4 |
