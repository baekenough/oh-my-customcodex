# 릴리즈 계획 — 2026-04-13 생성

> 출처: 2026-04-13 기준 `verify-done` 라벨 오픈 이슈
> 제외된 이슈 (이미 오픈 PR에 포함): 없음

## v0.86.0 릴리즈 계획

**예상 범위**: M | **이슈**: 1 | **병렬 가능**: 0

| # | 우선순위 | 규모 | 제목 | 의존성 |
|---|----------|------|------|--------|
| #841 | P2 | M | hada-scout 자동화: hada.io AI agent/harness 글 자동 /scout 스케줄링 (ubuntu-ext 서버) | 없음 |

### 구현 순서

1. #841 — hada-scout 자동화 스킬 + 스크래핑 스크립트 + cron 설정 (권장 에이전트: mgr-creator → 스킬 생성, lang-typescript-expert 또는 general-purpose → 스크립트 구현)

### 구현 세부 단계

1. hada.io RSS 피드 존재 여부 사전 확인 (`curl -s https://news.hada.io/rss`)
2. `.claude/skills/hada-scout/SKILL.md` 스킬 정의 (mgr-creator)
3. `hada-scout.sh` 스크래핑 + 필터링 + state 관리 스크립트 작성
4. `/scout` 연동 (Claude headless mode `claude -p`)
5. ubuntu-ext 배포 스크립트 (`install.sh`) + cron 등록
6. 설치/배포 문서 작성

### 참고 사항

- **Option B 채택**: CronCreate + 전용 스킬 (프로젝트 생태계 일관성)
- **RSS 우선 전략**: RSS 피드 존재 시 RSS, 없으면 HTML 파싱 fallback
- **리스크**: hada.io HTML 구조 변경 (High), API key 관리 (Medium), Claude headless 토큰 비용
- **보안 고려**: ubuntu-ext 서버 API key (Anthropic, GitHub) 관리 방안 필요
- **기존 검증**: `/scout` 스킬의 Pre-flight Guard 1/2 활용 가능
- **중복 체크 이중 실행**: hada-scout state 파일 + scout Guard 2 (성능 영향 미미)

## 요약

| 릴리즈 | 이슈 수 | 규모 | P1 | P2 | P3 |
|--------|---------|------|----|----|-----|
| v0.86.0 | 1 | M | 0 | 1 | 0 |
