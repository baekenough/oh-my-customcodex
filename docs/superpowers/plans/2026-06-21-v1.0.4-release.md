# v1.0.4 Auto-Dev Release Plan — Documentation-only compatibility review

Date: 2026-06-21
Sources: auto-dev release-monitor issues #1524, #1525, and #1526.

## Scope

| Issue | Priority | Effort | Decision | Local surface |
| --- | --- | --- | --- | --- |
| #1524 | P2 | S | Port Claude Code v2.1.178 compatibility notes from upstream oh-my-customcode v1.0.9 / #1391 as documentation-only guidance. | Claude compatibility guide, template mirror, wiki guide, changelog |
| #1525 | P2 | XS | Review `Yeachan-Heo/oh-my-codex` v0.18.13 and record no source/runtime change for this package. | Release plan and changelog |
| #1526 | P2 | S | Record OpenAI Codex `rust-v0.141.0` runtime compatibility impacts as documentation-only guidance. | OpenAI Codex compatibility guide, template mirror, wiki guide, changelog |

## Compression Mode

`compression_mode=lite`.

Rationale:

- Scope is three release-monitor issues.
- Changes are compatibility documentation, template mirrors, wiki guide summaries, and changelog entries.
- No TypeScript source, dependency, generated manifest, package metadata, runtime config, or migration is required.
- Existing `MINIMUM_OMX_VERSION` remains `0.18.0`; #1525 does not require a bump to v0.18.13 because this package does not consume a v0.18.13-only OMX API.

## Upstream Review: #1524 / Claude Code v2.1.178

Source evidence: upstream oh-my-customcode v1.0.9, issue #1391, PR #1392, and release PR #1393.

Documentation impact:

- R002-compatible note: Claude permission deny rules support per-parameter `Tool(param:value)` matching with `*` wildcards, for example `Agent(model:opus)`.
- R006-compatible notes: compaction honors `fallbackModel`; nested `.claude/skills` loads with `<dir>:<name>` disambiguation; MCP `mcp__*` entries in subagent `disallowedTools` are honored; nearest nested `.claude/` definitions win on name collisions.
- R010-compatible notes: auto mode classifies subagent spawns before launch; `claude agents` custom-gateway `401 Invalid bearer token` and `/bg` stuck-`Working` failures are fixed.

Codex-port decision:

- Record these as Claude compatibility notes only.
- Do not modify Codex/OMX runtime routing, permissions, or package source code.

## Upstream Review: #1525 / oh-my-codex v0.18.13

Upstream v0.18.13 is a patch release focused on project-scoped resume/search discovery, setup/hook safety, ralplan/autopilot freshness gates, CI/release reliability, sidecar/team state alignment, geobench documentation/schema fixes, and release-readiness cleanup.

Codex-port decision:

- No direct source or runtime port is required in `oh-my-customcodex`.
- The changed upstream implementation surfaces belong to the OMX runtime package, not this child package's shipped docs/templates/CLI surface.
- The existing `MINIMUM_OMX_VERSION = '0.18.0'` baseline remains sufficient; no bump to `0.18.13` is required.

## OpenAI Codex Review: #1526 / rust-v0.141.0

Record the release-note impact in the OpenAI Codex compatibility guide:

- Remote executors: authenticated end-to-end encrypted Noise relay channels plus cross-platform cwd/shell/filesystem-permission path preservation.
- Plugins/MCP/apps: selected-plugin stdio MCP activation, marketplace/auth catalog updates, child-thread listing, external-agent import details, and rate-limit reset credits.
- Realtime/TUI: explicit speech append, response-entry controls, omitted startup context, and auto-resolving prompts.
- Reliability/security: hook trust persistence, blocking `PostToolUse` rejection in code mode, plugin auth/dedup/order fixes, Windows sandbox and PowerShell timing fixes, exec relay keepalives, `wait_agent` interruption, SQLite WAL-reset fix, TLS P-521 support, and performance/cache cleanup.

Codex-port decision:

- Record runtime compatibility impacts only.
- Do not change package dependencies, source code, generated templates beyond the compatibility guide mirror, or runtime config.

## Implementation Notes

1. Add Unreleased changelog entries for #1524, #1525, and #1526.
2. Add Claude Code v2.1.178 to `guides/claude-code/15-version-compatibility.md` and mirror it byte-for-byte to `templates/guides/claude-code/15-version-compatibility.md`.
3. Add OpenAI Codex `rust-v0.141.0` to `guides/openai-codex/01-version-compatibility.md` and mirror it byte-for-byte to `templates/guides/openai-codex/01-version-compatibility.md`.
4. Update `wiki/guides/claude-code.md` and `wiki/guides/openai-codex.md` summaries.
5. Do not commit, branch, tag, push, close issues, or publish from this scope.

## Verification

- `cmp -s guides/claude-code/15-version-compatibility.md templates/guides/claude-code/15-version-compatibility.md`
- `cmp -s guides/openai-codex/01-version-compatibility.md templates/guides/openai-codex/01-version-compatibility.md`
- `git diff --check -- CHANGELOG.md docs/superpowers/plans/2026-06-21-v1.0.4-release.md guides/claude-code/15-version-compatibility.md templates/guides/claude-code/15-version-compatibility.md guides/openai-codex/01-version-compatibility.md templates/guides/openai-codex/01-version-compatibility.md wiki/guides/claude-code.md wiki/guides/openai-codex.md`
- `bun run .github/scripts/validate-docs.ts --programmatic-only`

Note: `bash .github/scripts/verify-wiki-sync.sh` may still require a `wiki/.source-hashes.json` refresh after the guide/wiki updates. That hash manifest is outside this issue's owned path list, so this documentation-only scope does not update it.
