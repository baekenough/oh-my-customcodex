# Release Plan - 2026-04-24

> Source: open `verify-done` issues as of 2026-04-24.
> Excluded issues already in open PRs: none.

## v0.4.0 Release Plan

**Estimated scope**: L | **Issues**: 5 | **Parallel possible**: 3

| # | Priority | Size | Title | Dependencies |
|---|----------|------|-------|--------------|
| #905 | P1 | M | Tracker checkpoint agent implementation | none |
| #906 | P2 | S | sdd-dev Harness Decision Record template | none |
| #907 | P2 | S | Context fork cap drift correction | #908 validates after correction |
| #908 | P2 | M | R006 fork-list/frontmatter cross-validation | #907 provides normalized expected list |
| #909 | P3 | XS | Auto-dev sensitive-path policy guidance | none |

### Implementation Order

1. #907 - Normalize R006 context-fork count/list to actual Codex skill frontmatter.
2. #908 - Add a deterministic fork-list verification script and wire sauron guidance to it.
3. #906 - Add `sdd/decisions/` workflow guidance and decision-record template.
4. #905 - Add `tracker-checkpoint` system agent and connect dag/pipeline guard docs.
5. #909 - Add Codex-native auto-dev sensitive-path policy note.

### Notes

- Version target is `v0.4.0` because the release adds managed runtime surface area: a new system agent, a new SDD template, and new verification capability.
- `#907` and `#908` are coupled: the verifier should run after R006 is normalized.
- Template mirrors under `templates/.claude/` must stay synchronized with the repo-local `.codex/` surface where this package expects template validation.

## Summary

| Release | Issues | Scope | P1 | P2 | P3 |
|---------|--------|-------|----|----|----|
| v0.4.0 | 5 | L | 1 | 3 | 1 |
