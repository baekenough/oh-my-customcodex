# v0.5.17 Release Plan — Codex compatibility and changelog gate

Date: 2026-06-09

## Scope

| issue | priority | decision | surface |
| --- | --- | --- | --- |
| #1484 | P3 | Implement release-note guard so future tags require a promoted `CHANGELOG.md` entry. | `.github/workflows/release.yml`, `workflows/auto-dev.yaml`, templates, tests |
| #1481 | P3 | Record OpenAI Codex rust-v0.138.0 impact where this package owns docs/templates. | `guides/openai-codex/**`, templates, guide index, tests |

## Compression decision

`compression_mode=lite`.

Evidence:
- Scope size is 2 issues.
- Changes are low-risk documentation, workflow guard text, release workflow shell behavior, and template parity.
- No dependency, migration, or security-sensitive runtime code changes are required.
- Full verify-build still runs before release.

## Implementation notes

- Backfill `CHANGELOG.md` for v0.5.16 and add v0.5.17 entries before tagging.
- Keep root `workflows/auto-dev.yaml` and `templates/workflows/auto-dev.yaml` identical.
- Keep OpenAI Codex guide source/template mirrors identical.
- Release workflow should fail on missing `CHANGELOG.md` or missing tagged version section instead of silently falling back to generated notes.

## Verification

- Targeted tests: release workflow + template validation.
- Sync checks: template, wiki, version.
- Full gate: `bun install`, lint, typecheck, full test suite, build.
