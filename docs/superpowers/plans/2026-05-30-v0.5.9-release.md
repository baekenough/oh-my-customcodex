# v0.5.9 Auto-Dev Release Plan

Date: 2026-05-30
Branch: `release/v0.5.9-auto-dev`
Source: `verify-done` open issues #1428-#1431.

**Expected scope**: M | **Issues**: 4 | **Release type**: auto-dev patch/minor readiness

| # | Priority | Size | Title | Dependencies |
|---|----------|------|-------|--------------|
| #1428 | P1 | XS/S | PR CI should run validate-docs | None |
| #1429 | P2 | M | Port v0.159.0 compatibility, retire codex-exec/gemini-exec/agora, document opt-in `openai/codex-plugin-cc` | #1428 |
| #1430 | P2 | S | Harden R018 standalone-agent fallback/deterministic verification/chunking | None |
| #1431 | P2 | M | Merge retrospective items into #1430 guidance: team status distrust, AskUserQuestion misuse, zsh status var | #1430 |

## Implementation Order

1. **#1428 — CI docs validation**: ensure PR CI runs `validate-docs` so documentation/template drift is caught before merge.
2. **#1430 + #1431 — R018 hardening**: update standalone-agent fallback guidance, deterministic verification expectations, chunking guidance, and retrospective learnings around team status distrust, AskUserQuestion misuse, and zsh status variable handling.
3. **#1429 — v0.159.0 compatibility and skill retirement**: port the compatible guidance, retire `codex-exec`, `gemini-exec`, and `agora`, and document opt-in use of `openai/codex-plugin-cc` without presenting it as default behavior.
4. **Version and lock metadata**: bump release metadata for v0.5.9 and regenerate lock/manifest outputs after source/template changes settle.
5. **Verification**: run the release gates below before PR validation and merge.

## Verification Gates

- `verify-template-sync`
- `verify-wiki-sync`
- `validate-docs`
- sensitive-output-guidance test
- validate-docs test
- hook script tests
- lint
- typecheck
- `bun test`
- build

## Notes

- A clean worktree was created from `origin/develop` because the local `release/v0.4.0-auto-dev` worktree was stale and dirty.
- No production publish should occur without CI and PR validation.
