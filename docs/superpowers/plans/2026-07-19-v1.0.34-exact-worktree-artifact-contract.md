# v1.0.34 Exact-Worktree and Artifact-Body Contract

## Scope

- Fix #1696 exact-merge verification so post-release gates enter, normalize, and assert the intended worktree and SHA in the same compound shell before install or tests.
- Make the deep-verify writer input contract explicit: callers omit every `deep-verify-counts` marker and the helper appends exactly one marker from structured findings.

## Sequence

1. Add focused regression assertions for both contracts and confirm they fail against the current guidance.
2. Update the R020 source/template pair, both auto-dev authoring surfaces and their mirrors, and the deep-verify skill mirrors.
3. Run focused artifact/workflow/lifecycle tests plus template parity validation.

## Completion Evidence

- Marker-free artifact input succeeds and produces exactly one exact count marker; caller-supplied markers retain the stable fail-closed diagnostic.
- Every post-release workflow prompt contains the same-shell normalized worktree guard in `cd` → root/PWD/HEAD assertions → install/test order.
- Source/template and source/plugin mirrors are byte-identical where required.
