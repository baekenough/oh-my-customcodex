# v1.0.5 Auto-Dev Release Plan — Upstream compatibility sweep

Date: 2026-07-04
Sources: auto-dev release-monitor issues #1556, #1557, #1558, and #1559.

## Scope

| Issue | Priority | Effort | Decision | Local surface |
| --- | --- | --- | --- | --- |
| #1556 | P2 | S | Adopt oh-my-codex v0.18.17 runtime reliability fixes by raising the packaged OMX baseline instead of copying runtime internals. | `src/core/omx-installer.ts`, installer/doctor tests, OpenAI Codex compatibility guide, template mirror, changelog |
| #1557 | P3 | XS | Review upstream oh-my-customcode v1.1.0 `grill-with-docs`; no direct Codex port because this child package does not ship the Claude-only skill surface and no local equivalent is required for this sweep. | Issue triage/comment only |
| #1558 | P2 | XS | Review upstream oh-my-customcode v1.1.1 Sonnet 5 harness cleanup; local Sonnet/model wording is covered by the v1.1.2 audit cleanup. | Source/template wording cleanup, changelog |
| #1559 | P2 | S | Port upstream v1.1.2 audit cleanup for stale model/delegation wording, obsolete `/tmp` artifact-bypass guidance, and generated metadata count drift. | Hooks, rules, skills, guides, templates, wiki hashes, tests |

## Compression Mode

`compression_mode=lite`.

Rationale:

- Scope is four release-monitor issues with low-risk compatibility, docs, template, hook-copy, test-copy, and package-baseline edits.
- One TypeScript runtime constant changes, but it is a dependency floor only; no new dependency, command, schema, or migration is introduced.
- Verification still requires the full build gate: install, lint, typecheck, full tests, docs validation, template/wiki sync, version sync, and build.

## Implementation Plan

1. Label and triage release-monitor issues without changing labels solely to qualify for compression.
2. Raise `MINIMUM_OMX_VERSION` to `0.18.17` and update installer/doctor test expectations.
3. Record oh-my-codex v0.18.17 compatibility evidence in `guides/openai-codex/01-version-compatibility.md` and mirror it to templates.
4. Refresh stale Claude-derived wording and generated metadata counts across source/template/wiki surfaces.
5. Promote changelog entries and bump `package.json` plus `templates/manifest.json` to `1.0.5`.
6. Run full verification, then branch, PR, publish, and close scoped issues with release evidence.

## Verification Checklist

- `bun install`
- `bun run lint`
- `bun run typecheck`
- `bun test`
- `bun run .github/scripts/validate-docs.ts --programmatic-only`
- `bash .github/scripts/verify-version-sync.sh`
- `bash .github/scripts/verify-template-sync.sh`
- `bash .github/scripts/verify-wiki-sync.sh`
- `bun run build`
- registry/release verification after publish
