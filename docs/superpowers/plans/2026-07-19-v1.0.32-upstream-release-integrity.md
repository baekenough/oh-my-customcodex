# v1.0.32 Upstream Compatibility and Release Integrity Plan

## Objective

Bundle issues #1688-#1692 into one release that preserves parent-package behavior while keeping Codex/OMX as the active provider boundary.

## Changes

1. Add provider-owned Claude Code v2.1.211, v2.1.212, and v2.1.214 compatibility records to mirrored rules, guides, Wiki pages, and regression tests.
2. Teach R020 to distinguish a successful CI conclusion from actual full execution, using this child's `full_ci=false` and `Report documentation-only fast path` evidence.
3. Lock R009's verification-Bash/action-delegate same-message dispatch invariant.
4. Activate the release-PR Verdaccio workflow with a real ephemeral token and `--ignore-scripts`, preserving child package and command names.
5. Run Wiki drift verification before `reviewedTree` freeze, remove Auto Tag's duplicate remote-ref deletion, and require bounded explicit-Bash readback owned by the pipeline.

## Verification

- Red/green targeted tests for compatibility, CI semantics, deploy-test activation, pre-freeze Wiki ordering, bounded polling, and single deletion ownership.
- Version sync, Wiki sync, template sync, lint, typecheck, full test suite, build, and package contract.
- Freeze one reviewed Git tree and pass the exact binary diff to six independent review angles before the single Lore commit.
- Merge only after the full check rollup passes at the final PR head, including the latest non-skipped successful `Deploy Test with Verdaccio` check run while allowing older rerun history, then verify tag ancestry, both registries, GitHub Release evidence, closed issues/milestone, and remote-ref absence.

## Stop Condition

The reviewed tree is immutable, every local and CI gate passes, #1688-#1692 close through v1.0.32 automation, public npm and GitHub Packages report 1.0.32 at the merge SHA, and the eligible issue/PR queues are re-enumerated after homework.
