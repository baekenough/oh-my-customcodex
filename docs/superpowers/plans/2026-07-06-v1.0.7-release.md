# v1.0.7 Auto-Dev Release Plan — 2026-07-06

## Scope

Process the current auto-dev-eligible upstream release monitor issue:

| Issue | Upstream | Disposition |
|-------|----------|-------------|
| #1567 | baekenough/oh-my-customcode v1.1.7 | Port Claude Code v2.1.200-v2.1.201 compatibility notes across R002/R006/R010/R020, template mirrors, Claude compatibility guide, and wiki summaries. |

## Implementation Plan

1. Update source rules under `.codex/rules/` and mirror `templates/.claude/rules/`.
2. Update `guides/claude-code/15-version-compatibility.md` and template mirror.
3. Update wiki rule/guide summaries and regenerate source hashes.
4. Promote changelog and release metadata to v1.0.7.
5. Run version/template/wiki/docs/test/build verification before release.

## Verification Plan

- `bun install`
- `bash .github/scripts/verify-template-sync.sh`
- `bash .github/scripts/verify-wiki-sync.sh`
- `bash .github/scripts/verify-version-sync.sh`
- `bun run .github/scripts/validate-docs.ts --programmatic-only`
- `bun run lint`
- `bun run typecheck`
- `bun test`
- `bun run build`
- `git diff --check`

## Release Decision

Patch release (`1.0.7`): compatibility notes, rule/doc hardening, template mirrors, and wiki metadata without user-facing breaking CLI/API changes.
