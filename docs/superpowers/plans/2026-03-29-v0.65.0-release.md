# Release Plan — Generated 2026-03-29

> Source: open issues labeled `verify-done` as of 2026-03-29
> Issues excluded (already in open PRs): none
> Parent/tracking issues excluded: #723 (closes when #725 completes)

## v0.65.0 Release Plan

**Estimated scope**: L | **Issues**: 1 | **Parallelizable**: 2 (Phase C and D are partially parallelizable)

| # | Priority | Size | Title | Dependencies |
|---|----------|------|-------|-------------|
| #725 | P2 | L | [cc-native] v0.65.0 — Hook Registry Expansion + CC Feature Integration | Parent: #723 |

### Implementation Order

1. **Phase C: Hook Registry Expansion** (suggested agent: mgr-creator / mgr-claude-code-bible)
   - Document all 25+ hook event types in R006
   - Implement high-value hooks: UserPromptSubmit, CwdChanged, FileChanged, Notification
   - Document `http` and `agent` hook handler types
   - Add TeammateIdle, TaskCreated, TaskCompleted for Agent Teams

2. **Phase D: CC Feature Integration** (suggested agent: mgr-claude-code-bible / arch-documenter)
   - Document CLI flags: --worktree, --remote, --bare, --agent, --from-pr, --chrome, --json-schema, --max-budget-usd, --fallback-model
   - Document headless mode (claude -p) and CronCreate scheduling
   - Evaluate claude-code-action for CI integration
   - Document auto mode configuration (autoMode.environment, allow/soft_deny)
   - Update monitoring-setup SKILL.md with new OTel capabilities
   - Add data privacy env vars (DISABLE_TELEMETRY, etc.)

### Acceptance Criteria
- [ ] R006 hook section lists all 25+ event types
- [ ] 3+ new hook types implemented in hooks.json
- [ ] CLI flags documented in guide or rule
- [ ] monitoring-setup SKILL.md updated
- [ ] Templates synced, tests pass

### Notes
- L-sized issue — standalone release unit
- Phase C (hooks) and Phase D (features) are partially parallelizable
- #723 is parent tracking issue — recommend close after #725 merges
- This release absorbs #721 (Hook expansion, previously closed as duplicate)

## Summary

| Release | Issues | Size | P1 | P2 | P3 |
|---------|--------|------|----|----|-----|
| v0.65.0 | 1 | L | 0 | 1 | 0 |

### Post-Release
- Close #723 (parent tracker) after #725 merge confirmed
